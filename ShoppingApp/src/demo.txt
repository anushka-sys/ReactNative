Great — I went through your src structure carefully. You’ve already done a solid job 👏 (navigation, API, screens are cleanly separated). Now we’ll plug in Cart using Context API in a way that fits your current structure.

I’ll guide you step-by-step exactly as you asked — no skipping.

🔥 BIG PICTURE (How Cart will work)

Right now:

Home → shows products ✅
ProductDetails → receives product ✅

We’ll add:

Global Cart State (Context API)
Any screen can:
Add item
Update quantity
Read cart
✅ STEP 1: Fix & Structure Your Context

You already have:

src/context/Context.js

We will upgrade this file instead of creating new ones.

✨ 1.1 Replace your Context.js with this

import React, { createContext, useState } from 'react';

// Create Context
export const CartContext = createContext();

// Provider Component
export const CartProvider = ({ children }) => {
  // Cart State
  const [cartItems, setCartItems] = useState([]);

  // 🧠 Add to Cart
  const addToCart = (product) => {
    const existingItem = cartItems.find(item => item.id === product.id);

    if (existingItem) {
      // If already exists → increase quantity
      const updatedCart = cartItems.map(item =>
        item.id === product.id
          ? { ...item, quantity: item.quantity + 1 }
          : item
      );
      setCartItems(updatedCart);
    } else {
      // New product
      setCartItems([...cartItems, { ...product, quantity: 1 }]);
    }
  };

  // ❌ Remove Item
  const removeFromCart = (productId) => {
    const filtered = cartItems.filter(item => item.id !== productId);
    setCartItems(filtered);
  };

  // ➕ Increase Quantity
  const increaseQuantity = (productId) => {
    const updated = cartItems.map(item =>
      item.id === productId
        ? { ...item, quantity: item.quantity + 1 }
        : item
    );
    setCartItems(updated);
  };

  // ➖ Decrease Quantity
  const decreaseQuantity = (productId) => {
    const updated = cartItems
      .map(item =>
        item.id === productId
          ? { ...item, quantity: item.quantity - 1 }
          : item
      )
      .filter(item => item.quantity > 0); // remove if 0

    setCartItems(updated);
  };

  // 🧹 Clear Cart
  const clearCart = () => {
    setCartItems([]);
  };

  // 💰 Total Price
  const getTotalPrice = () => {
    return cartItems.reduce(
      (total, item) => total + item.price * item.quantity,
      0
    );
  };

  return (
    <CartContext.Provider
      value={{
        cartItems,
        addToCart,
        removeFromCart,
        increaseQuantity,
        decreaseQuantity,
        clearCart,
        getTotalPrice,
      }}
    >
      {children}
    </CartContext.Provider>
  );
};

🧠 WHY this is needed
Concept	Why
createContext()	Creates global store
CartProvider	Wraps entire app
cartItems	Stores all cart data
quantity	Needed for duplicates
functions	Handle all cart logic centrally
✅ STEP 2: Wrap Your App with Provider

Go to:

src/navigation/AppNavigator.js

OR your root App.js (where navigation is rendered)

✨ Modify it like this:

import React from 'react';
import { NavigationContainer } from '@react-navigation/native';
import AppNavigator from './src/navigation/AppNavigator';
import { CartProvider } from './src/context/Context';

const App = () => {
  return (
    <CartProvider>
      <NavigationContainer>
        <AppNavigator />
      </NavigationContainer>
    </CartProvider>
  );
};

export default App;

🧠 WHY

Without this:

Context won’t be available in screens ❌

With this:

Any screen can access cart ✅
✅ STEP 3: Use Context in ProductDetails

File:

src/screens/ProductDetails.js
✨ 3.1 Import
import React, { useContext } from 'react';
import { CartContext } from '../context/Context';
✨ 3.2 Inside Component
const { addToCart } = useContext(CartContext);
✨ 3.3 Add Button

Find your UI button and update:

<TouchableOpacity
  onPress={() => addToCart(product)}
  style={{ backgroundColor: 'blue', padding: 12 }}
>
  <Text style={{ color: '#fff' }}>Add to Cart</Text>
</TouchableOpacity>
🧠 WHAT happens now
If product is new → added
If already exists → quantity increases
✅ STEP 4: Build Cart Screen

File:

src/screens/CartScreen.js
✨ Full Working Example


import React, { useContext } from 'react';
import { View, Text, FlatList, Image, TouchableOpacity } from 'react-native';
import { CartContext } from '../context/Context';

const CartScreen = () => {
  const {
    cartItems,
    increaseQuantity,
    decreaseQuantity,
    removeFromCart,
    getTotalPrice,
  } = useContext(CartContext);

  return (
    <View style={{ flex: 1, padding: 10 }}>
      
      <FlatList
        data={cartItems}
        keyExtractor={(item) => item.id.toString()}
        renderItem={({ item }) => (
          <View style={{ marginBottom: 15, borderBottomWidth: 1 }}>

            <Image
              source={{ uri: item.image }}
              style={{ height: 100, width: 100 }}
            />

            <Text>{item.title}</Text>
            <Text>₹ {item.price}</Text>

            {/* Quantity Controls */}
            <View style={{ flexDirection: 'row', alignItems: 'center' }}>
              <TouchableOpacity onPress={() => decreaseQuantity(item.id)}>
                <Text>-</Text>
              </TouchableOpacity>

              <Text style={{ marginHorizontal: 10 }}>
                {item.quantity}
              </Text>

              <TouchableOpacity onPress={() => increaseQuantity(item.id)}>
                <Text>+</Text>
              </TouchableOpacity>
            </View>

            {/* Remove */}
            <TouchableOpacity onPress={() => removeFromCart(item.id)}>
              <Text style={{ color: 'red' }}>Remove</Text>
            </TouchableOpacity>

          </View>
        )}
      />

      {/* Total */}
      <Text style={{ fontSize: 18, fontWeight: 'bold' }}>
        Total: ₹ {getTotalPrice()}
      </Text>

    </View>
  );
};

export default CartScreen;


✅ STEP 5: Checkout Screen

File:

src/screens/CheckoutPage.js
✨ Logic

Same as cart BUT:

Show item total
Show grand total
✨ Code

const { cartItems, increaseQuantity, decreaseQuantity, getTotalPrice } = useContext(CartContext);

{cartItems.map(item => (
  <View key={item.id}>
    <Text>{item.title}</Text>

    <Text>Price: ₹ {item.price}</Text>

    <Text>Total: ₹ {item.price * item.quantity}</Text>

    <View style={{ flexDirection: 'row' }}>
      <Button title="-" onPress={() => decreaseQuantity(item.id)} />
      <Text>{item.quantity}</Text>
      <Button title="+" onPress={() => increaseQuantity(item.id)} />
    </View>
  </View>
))}

<Text>Grand Total: ₹ {getTotalPrice()}</Text>

⚠️ COMMON MISTAKES (VERY IMPORTANT)
❌ 1. Cart resets on navigation

✔ Fix:

Ensure CartProvider wraps entire app
❌ 2. Duplicate items issue

✔ Fix:

Use find() in addToCart
❌ 3. Quantity goes negative

✔ Fix:

Filter items with quantity > 0
❌ 4. UI not updating

✔ Fix:

Never mutate state directly
Always use setCartItems([...])
🧠 DEBUG TIPS

Add this anywhere:

console.log(cartItems);

If empty:
👉 Context not connected

🏁 FINAL STRUCTURE (AFTER IMPLEMENTATION)
src/
 ├── context/
 │    └── Context.js ✅
 ├── screens/
 │    ├── ProductDetails.js ✅
 │    ├── CartScreen.js ✅
 │    ├── CheckoutPage.js ✅
🚀 NEXT LEVEL (after this works)

Tell me when you're done, I’ll help you add:

🔥 Cart badge count on header
💾 Save cart in AsyncStorage (persistent)
⚡ Redux version (for interviews)

If you want, I can also:
👉 Modify YOUR exact files line-by-line based on your code (more precise)

Just say: "review my ProductDetails code" 👍